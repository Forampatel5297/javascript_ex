const text = 'game of thrones';
const firstChar = ' the first char from the text variable';

const secondChar ='the char at index 5 from the text variable';

const thirdChar = 'the char at index 8 from the text variable';

const result = firstChar.concat(secondChar).concat(thirdChar);

console.log(result);
const t1 = result.toUpperCase();
console.log(t1);
const text2 ='the final result is: GOT';
console.log(text2);