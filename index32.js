const height = 10;
const base = 4;

const perimeterofrectangle = 2 * height+base;

const areaofrectangle = height * base;

console.log(`permeter of rectangle output: ${perimeterofrectangle}` );

console.log(`area of rectangular output is:${areaofrectangle}`);
