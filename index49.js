const firstNumber = 4;
const secondNumber = 5;
const result = firstNumber > secondNumber

if(result){
    console.log('the' +
    firstNumber+ 'is bigger than the' +secondNumber +'...');
 }

 console.log('the is first Number is  bigger than the second Number ...' , result);

