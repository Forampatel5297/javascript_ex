const firstside = 10;
const secondside = 20;
const thirdside = 5;

const perimeteroftringle = firstside + secondside + thirdside;

console.log(`the triangle perimeter and sides length is:${perimeteroftringle}`);